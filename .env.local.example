# Copy this file to .env.local and adjust if your MongoDB server isn't on
# the default local port. Both variables are optional — these are the
# defaults used when they're not set.

MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DB=greentiq_crm
